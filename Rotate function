Problem 4 :You are given an integer array nums of length n. Return maximum length of Rotation Function.
problem link:https://leetcode.com/problems/rotate-function/description/
Code:-
  int maxRotateFunction(vector<int>& arr) {
        int tot_sum=0;
        int sum=0;
        int n=arr.size();
        for(int i=0;i<n;i++){
         tot_sum+= arr[i];
          sum+= i*arr[i];
        }
        int maxi=sum;
        for(int k=1;k<n;k++){
          sum=sum+tot_sum-n*arr[n-k];
          maxi=max(sum,maxi);
        }
        return maxi;
    }
    
    This function will take time complexity of O(n) and space complexity of O(1).
